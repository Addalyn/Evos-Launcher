From 237f6ea826f05bca2317f20340061dc8647c37f2 Mon Sep 17 00:00:00 2001
From: Addalyn <dev@addalyn.baby>
Date: Wed, 25 Feb 2026 15:32:37 +0100
Subject: [PATCH] [3.2.2] - 2026-02-25

## Features & Improvements

- **Block User Feature**:
  - Ability to block/unblock players directly from the Chat page or Player Stats page.
  - Messages from blocked users are automatically filtered from the chat history.
  - Notifications and unread message counts are suppressed for blocked users.
  - Outgoing messages to blocked users are disabled with a status indicator in the chat input.
- **UI & UX Enhancements**:
  - Added a chat warning message to report abuse via Discord.
  - Fixed "VIEW QUESTS" button rendering on the profile page to prevent text wrapping.
  - Improved layout and spacing in the Chat page header.
- **Enhanced Global Localization**:
  - Full translation support for blocking features and chat warnings across all 10 supported languages.
---
 CHANGELOG.md                                  |  16 ++
 assets/splash.html                            |   2 +-
 package.json                                  |   2 +-
 release/app/package-lock.json                 |   4 +-
 release/app/package.json                      |   2 +-
 src/renderer/components/atlas/Player.tsx      |   7 +
 .../components/generic/ChatContext.tsx        |   7 +-
 .../generic/navbar/navigationConfig.ts        |  17 +-
 .../components/generic/navbar/useNavbar.ts    |   3 +-
 src/renderer/components/pages/ChatPage.tsx    | 220 ++++++++++++++----
 .../components/pages/PlayerStatsPage.tsx      |  32 ++-
 .../components/pages/SettingsPage.tsx         |   4 +
 .../components/settings/AppearanceSection.tsx |  12 +
 src/renderer/hooks/useChatWebSocket.ts        |   7 +
 src/renderer/lib/EvosStore.ts                 |  37 +++
 src/renderer/locales/br/translation.json      |   9 +-
 src/renderer/locales/de/translation.json      |  11 +-
 src/renderer/locales/en/translation.json      |   9 +-
 src/renderer/locales/es/translation.json      |   9 +-
 src/renderer/locales/fr/translation.json      |   9 +-
 src/renderer/locales/it/translation.json      |   9 +-
 src/renderer/locales/nl/translation.json      |   9 +-
 src/renderer/locales/ru/translation.json      |   9 +-
 src/renderer/locales/tr/translation.json      |   9 +-
 src/renderer/locales/zh/translation.json      |   9 +-
 25 files changed, 376 insertions(+), 88 deletions(-)

diff --git a/CHANGELOG.md b/CHANGELOG.md
index fd255c4..15b7ded 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,21 @@
 # Evos Launcher Changelog
 
+# [3.2.2] - 2026-02-25
+
+## Features & Improvements
+
+- **Block User Feature**:
+  - Ability to block/unblock players directly from the Chat page or Player Stats page.
+  - Messages from blocked users are automatically filtered from the chat history.
+  - Notifications and unread message counts are suppressed for blocked users.
+  - Outgoing messages to blocked users are disabled with a status indicator in the chat input.
+- **UI & UX Enhancements**:
+  - Added a chat warning message to report abuse via Discord.
+  - Fixed "VIEW QUESTS" button rendering on the profile page to prevent text wrapping.
+  - Improved layout and spacing in the Chat page header.
+- **Enhanced Global Localization**:
+  - Full translation support for blocking features and chat warnings across all 10 supported languages.
+
 # [3.2.1] - 2026-02-24
 
 ## Features & Improvements
diff --git a/assets/splash.html b/assets/splash.html
index c11e3dd..bf6e657 100644
--- a/assets/splash.html
+++ b/assets/splash.html
@@ -372,7 +372,7 @@ <h1 class="title">EVOS LAUNCHER</h1>
   </div>
 
   <!-- Version -->
-  <div class="version">v3.2.1</div>
+  <div class="version">v3.2.2</div>
 
   <script>
     // Create floating particles
diff --git a/package.json b/package.json
index fc840a2..7b84afa 100644
--- a/package.json
+++ b/package.json
@@ -1,5 +1,5 @@
 {
-  "version": "3.2.1",
+  "version": "3.2.2",
   "description": "An Atlas Reactor Launcher",
   "keywords": [
     "Atlas Reacor"
diff --git a/release/app/package-lock.json b/release/app/package-lock.json
index 53259c6..0264dce 100644
--- a/release/app/package-lock.json
+++ b/release/app/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "evoslauncher",
-  "version": "3.2.1",
+  "version": "3.2.2",
   "lockfileVersion": 2,
   "requires": true,
   "packages": {
     "": {
       "name": "evoslauncher",
-      "version": "3.2.1",
+      "version": "3.2.2",
       "hasInstallScript": true,
       "license": "MIT",
       "dependencies": {
diff --git a/release/app/package.json b/release/app/package.json
index d66509e..dc71e6d 100644
--- a/release/app/package.json
+++ b/release/app/package.json
@@ -1,6 +1,6 @@
 {
   "name": "evoslauncher",
-  "version": "3.2.1",
+  "version": "3.2.2",
   "description": "An Atlas Reactor Launcher built with Electron, React",
   "license": "MIT",
   "author": {
diff --git a/src/renderer/components/atlas/Player.tsx b/src/renderer/components/atlas/Player.tsx
index 0c46a6e..0d71c12 100644
--- a/src/renderer/components/atlas/Player.tsx
+++ b/src/renderer/components/atlas/Player.tsx
@@ -57,6 +57,8 @@ interface Props {
   characterType: string | undefined;
   /** Legacy title parameter */
   titleOld: string;
+  /** Custom click handler */
+  onClick?: () => void;
 }
 
 /**
@@ -211,6 +213,7 @@ function Player({
   disableSkew = false,
   characterType = undefined,
   titleOld = '',
+  onClick = undefined,
 }: Props) {
   const { t } = useTranslation();
   const [specialNames, setSpecialNames] = useState<SpecialNames>();
@@ -231,6 +234,10 @@ function Player({
    * Navigates to player statistics if valid player data exists
    */
   const handleClick = (): void => {
+    if (onClick) {
+      onClick();
+      return;
+    }
     if (!info) {
       return;
     }
diff --git a/src/renderer/components/generic/ChatContext.tsx b/src/renderer/components/generic/ChatContext.tsx
index ec27454..88628d3 100644
--- a/src/renderer/components/generic/ChatContext.tsx
+++ b/src/renderer/components/generic/ChatContext.tsx
@@ -35,15 +35,17 @@ const ChatContext = createContext<ChatContextType | undefined>(undefined);
 
 export function ChatProvider({ children }: { children: React.ReactNode }) {
   const activeUser = EvosStore((state: any) => state.activeUser);
+  const hideChat = EvosStore((state: any) => state.hideChat);
   const [activeConversation, setActiveConversation] = useState<string | null>(
     null,
   );
 
   const { messages, sendMessage, readyState, onlineUsers } = useChatWebSocket({
     handle: activeUser?.handle,
-    enabled: !!activeUser,
+    enabled: !!activeUser && hideChat !== 'true',
   });
 
+  const blockedPlayers = EvosStore((state: any) => state.blockedPlayers);
   const [unreadCounts, setUnreadCounts] = useState<Record<string, number>>({});
   const lastProcessedIdRef = useRef<string | null>(null);
 
@@ -56,10 +58,11 @@ export function ChatProvider({ children }: { children: React.ReactNode }) {
       if (lastMessage.id === lastProcessedIdRef.current) return;
       lastProcessedIdRef.current = lastMessage.id;
 
-      // Only count if it's not from us, not system, and not the active conversation
+      // Only count if it's not from us, not system, not from a blocked player, and not the active conversation
       if (
         lastMessage.from !== activeUser?.handle &&
         lastMessage.from !== 'System' &&
+        !blockedPlayers.includes(lastMessage.from) &&
         lastMessage.from !== activeConversation
       ) {
         setUnreadCounts((prev) => ({
diff --git a/src/renderer/components/generic/navbar/navigationConfig.ts b/src/renderer/components/generic/navbar/navigationConfig.ts
index 08acdcf..328c234 100644
--- a/src/renderer/components/generic/navbar/navigationConfig.ts
+++ b/src/renderer/components/generic/navbar/navigationConfig.ts
@@ -39,6 +39,7 @@ import { electronFeatures } from 'renderer/utils/electronUtils';
 const createNavigationPages = (
   t: (key: string, options?: any) => string,
   isDev: boolean,
+  hideChat: string,
 ): NavigationPage[] => {
   const basePages: NavigationPage[] = [
     {
@@ -54,6 +55,16 @@ const createNavigationPages = (
       icon: React.createElement(Home),
       authentication: false, // No authentication required for status page
     },
+    ...(hideChat !== 'true'
+      ? [
+          {
+            title: t('menuOptions.chat', 'Chat'),
+            href: '/chat',
+            icon: React.createElement(Chat),
+            authentication: true,
+          },
+        ]
+      : []),
     {
       title: t('menuOptions.gstats'),
       href: '/stats',
@@ -84,12 +95,6 @@ const createNavigationPages = (
       icon: React.createElement(People),
       authentication: true, // Requires authentication for followed players
     },
-    {
-      title: t('menuOptions.chat', 'Chat'),
-      href: '/chat',
-      icon: React.createElement(Chat),
-      authentication: true,
-    },
     // Electron-only navigation items
     ...(electronFeatures.isAvailable
       ? [
diff --git a/src/renderer/components/generic/navbar/useNavbar.ts b/src/renderer/components/generic/navbar/useNavbar.ts
index 816e944..ac555ba 100644
--- a/src/renderer/components/generic/navbar/useNavbar.ts
+++ b/src/renderer/components/generic/navbar/useNavbar.ts
@@ -82,6 +82,7 @@ export default function useNavbar() {
     setOldBranch,
     oldBranch,
     setNoBranchDownload,
+    hideChat,
   } = evosStore;
 
   // Check developer status when active user changes
@@ -175,7 +176,7 @@ export default function useNavbar() {
   }, []);
 
   // Memoized values
-  const pages = createNavigationPages(t, isDev);
+  const pages = createNavigationPages(t, isDev, hideChat);
   const drawerWidth = width !== null && width < 916 ? 60 : 240;
 
   // Handlers
diff --git a/src/renderer/components/pages/ChatPage.tsx b/src/renderer/components/pages/ChatPage.tsx
index f746361..4192fd5 100644
--- a/src/renderer/components/pages/ChatPage.tsx
+++ b/src/renderer/components/pages/ChatPage.tsx
@@ -5,7 +5,7 @@
  * @since 3.2.1
  */
 
-import React, { useState, useEffect, useRef } from 'react';
+import React, { useState, useEffect, useRef, memo } from 'react';
 import {
   Box,
   Typography,
@@ -20,7 +20,9 @@ import {
   Fade,
   Tooltip,
   Skeleton,
+  Button,
 } from '@mui/material';
+import { useLocation, useNavigate } from 'react-router-dom';
 import {
   Send as SendIcon,
   Circle as CircleIcon,
@@ -34,8 +36,114 @@ import EvosStore from '../../lib/EvosStore';
 import Player from '../atlas/Player';
 import { getPlayerData, PlayerData } from 'renderer/lib/Evos';
 
+/**
+ * Optimized component for individual chat user list items
+ */
+const ChatUserItem = memo(
+  ({
+    user,
+    isSelected,
+    onClick,
+    token,
+    unreadCount,
+  }: {
+    user: string;
+    isSelected: boolean;
+    onClick: (u: string) => void;
+    token: string;
+    unreadCount: number;
+  }) => {
+    const [userData, setUserData] = useState<PlayerData>();
+
+    useEffect(() => {
+      let isMounted = true;
+      getPlayerData(token, user)
+        // eslint-disable-next-line promise/always-return
+        .then((resp) => {
+          if (isMounted) {
+            resp.data.status = resp.data.titleId as unknown as string;
+            setUserData(resp.data);
+          }
+        })
+        .catch((e) => {
+          // eslint-disable-next-line no-console
+          console.log(e);
+        });
+      return () => {
+        isMounted = false;
+      };
+    }, [user, token]);
+
+    return (
+      <ListItem key={user} disablePadding>
+        <ListItemButton
+          selected={isSelected}
+          onClick={() => onClick(user)}
+          sx={{
+            py: 0.1,
+            px: 0.1,
+            borderLeft: isSelected
+              ? '4px solid #ff7b00'
+              : '4px solid transparent',
+            '&.Mui-selected': {
+              backgroundColor: 'rgba(255, 123, 0, 0.1)',
+              '&:hover': { backgroundColor: 'rgba(255, 123, 0, 0.2)' },
+            },
+            '&:hover': { backgroundColor: 'rgba(255, 255, 255, 0.05)' },
+          }}
+        >
+          <Box sx={{ flex: 1, position: 'relative' }}>
+            {!userData ? (
+              <Skeleton
+                variant="rectangular"
+                width="100%"
+                height={52}
+                sx={{ borderRadius: 1, opacity: 0.1 }}
+              />
+            ) : (
+              <Player
+                info={userData}
+                disableSkew
+                characterType={undefined}
+                titleOld=""
+                onClick={() => onClick(user)}
+              />
+            )}
+            {unreadCount > 0 && (
+              <Box
+                sx={{
+                  position: 'absolute',
+                  top: -6,
+                  right: -6,
+                  minWidth: 18,
+                  height: 18,
+                  bgcolor: '#ff7b00',
+                  color: 'white',
+                  borderRadius: '50%',
+                  display: 'flex',
+                  alignItems: 'center',
+                  justifyContent: 'center',
+                  fontSize: '0.65rem',
+                  fontWeight: 'bold',
+                  boxShadow: '0 2px 5px rgba(0,0,0,0.5)',
+                  zIndex: 2,
+                  px: 0.5,
+                }}
+              >
+                {unreadCount}
+              </Box>
+            )}
+          </Box>
+        </ListItemButton>
+      </ListItem>
+    );
+  },
+);
+
 export default function ChatPage() {
   const { t } = useTranslation();
+  const navigate = useNavigate();
+  const hideChat = EvosStore((state: any) => state.hideChat);
   const activeUser = EvosStore((state: any) => state.activeUser);
   const {
     messages,
@@ -47,10 +155,23 @@ export default function ChatPage() {
     setActiveConversation,
   } = useChat();
 
+  const blockedPlayers = EvosStore((state: any) => state.blockedPlayers);
+  const addBlockedPlayer = EvosStore((state: any) => state.addBlockedPlayer);
+  const removeBlockedPlayer = EvosStore(
+    (state: any) => state.removeBlockedPlayer,
+  );
+
   const [selectedUser, setSelectedUser] = useState<string>('');
   const [inputText, setInputText] = useState('');
   const [searchTerm, setSearchTerm] = useState('');
   const messagesEndRef = useRef<HTMLDivElement>(null);
+  // Redirect if chat is hidden
+  useEffect(() => {
+    if (hideChat === 'true') {
+      navigate('/');
+    }
+  }, [hideChat, navigate]);
+
   /** Player data retrieved from the API */
   const [playerData, setPlayerData] = useState<PlayerData>();
 
@@ -72,6 +193,7 @@ export default function ChatPage() {
 
   useEffect(() => {
     if (selectedUser === '') {
+      setPlayerData(undefined);
       return;
     }
 
@@ -103,9 +225,10 @@ export default function ChatPage() {
 
   const conversationMessages = messages.filter(
     (m) =>
-      m.isSystem ||
-      (m.from === selectedUser && m.to === activeUser?.handle) ||
-      (m.from === activeUser?.handle && m.to === selectedUser),
+      !blockedPlayers.includes(m.from) &&
+      (m.isSystem ||
+        (m.from === selectedUser && m.to === activeUser?.handle) ||
+        (m.from === activeUser?.handle && m.to === selectedUser)),
   );
 
   const getConnectionStatusColor = () => {
@@ -134,7 +257,7 @@ export default function ChatPage() {
       {/* Users Sidebar */}
       <Paper
         sx={{
-          width: 300,
+          width: 265,
           display: 'flex',
           flexDirection: 'column',
           background: 'rgba(20, 20, 30, 0.6)',
@@ -163,10 +286,10 @@ export default function ChatPage() {
             variant="subtitle1"
             sx={{
               color: 'white',
-              mb: 2,
+              mb: 1,
               display: 'flex',
               alignItems: 'center',
-              gap: 1,
+              gap: 0,
             }}
           >
             {t('chat.subtitle')}
@@ -208,43 +331,14 @@ export default function ChatPage() {
             </Box>
           ) : (
             filteredUsers.map((user) => (
-              <ListItem key={user} disablePadding>
-                <ListItemButton
-                  selected={selectedUser === user}
-                  onClick={() => setSelectedUser(user)}
-                  sx={{
-                    py: 1.5,
-                    borderLeft:
-                      selectedUser === user
-                        ? '4px solid #ff7b00'
-                        : '4px solid transparent',
-                    '&.Mui-selected': {
-                      backgroundColor: 'rgba(255, 123, 0, 0.1)',
-                      '&:hover': { backgroundColor: 'rgba(255, 123, 0, 0.2)' },
-                    },
-                    '&:hover': { backgroundColor: 'rgba(255, 255, 255, 0.05)' },
-                  }}
-                >
-                  <ListItemText
-                    primary={user}
-                    primaryTypographyProps={{
-                      sx: {
-                        color: 'white',
-                        fontWeight: unreadCounts[user] ? 'bold' : 'normal',
-                      },
-                    }}
-                    secondary={
-                      unreadCounts[user]
-                        ? t('chat.newMessages', 'New messages')
-                        : ''
-                    }
-                    secondaryTypographyProps={{
-                      sx: { color: '#ff7b00', fontSize: '0.75rem' },
-                    }}
-                  />
-                  <CircleIcon sx={{ fontSize: 10, color: '#4caf50', ml: 1 }} />
-                </ListItemButton>
-              </ListItem>
+              <ChatUserItem
+                key={user}
+                user={user}
+                isSelected={selectedUser === user}
+                onClick={setSelectedUser}
+                token={activeUser?.token ?? ''}
+                unreadCount={unreadCounts[user] || 0}
+              />
             ))
           )}
         </List>
@@ -336,11 +430,32 @@ export default function ChatPage() {
               <br />
               <Typography
                 variant="subtitle2"
-                sx={{ color: 'white', fontWeight: 'bold' }}
+                sx={{ color: 'white', fontWeight: 'bold', flex: 1, fontSize: 12 }}
               >
-                {t('chat.notsaved')}
+                {t('chat.notsaved')}<br />
+                {t('chat.warning')}
               </Typography>
-            </Box>
+            {selectedUser && selectedUser !== activeUser?.handle && (
+              <Button
+                variant="outlined"
+                color={
+                  blockedPlayers.includes(selectedUser) ? 'error' : 'warning'
+                }
+                onClick={() => {
+                  if (blockedPlayers.includes(selectedUser)) {
+                    removeBlockedPlayer(selectedUser);
+                  } else {
+                    addBlockedPlayer(selectedUser);
+                  }
+                }}
+                sx={{ ml: 'auto' }}
+              >
+                {blockedPlayers.includes(selectedUser)
+                  ? t('menuOptions.Unblock')
+                  : t('menuOptions.Block')}
+              </Button>
+            )}
+          </Box>
 
             {/* Chat Messages */}
             <Box
@@ -422,7 +537,12 @@ export default function ChatPage() {
                   fullWidth
                   multiline
                   maxRows={4}
-                  placeholder={t('chat.placeholder', 'Type a message...')}
+                  disabled={blockedPlayers.includes(selectedUser)}
+                  placeholder={
+                    blockedPlayers.includes(selectedUser)
+                      ? t('chat.blockedUser', 'You have blocked this user')
+                      : t('chat.placeholder', 'Type a message...')
+                  }
                   value={inputText}
                   onChange={(e) => setInputText(e.target.value)}
                   onKeyPress={(e) => {
@@ -441,7 +561,9 @@ export default function ChatPage() {
                 />
                 <IconButton
                   onClick={handleSend}
-                  disabled={!inputText.trim()}
+                  disabled={
+                    !inputText.trim() || blockedPlayers.includes(selectedUser)
+                  }
                   sx={{
                     bgcolor: '#ff7b00',
                     color: 'white',
diff --git a/src/renderer/components/pages/PlayerStatsPage.tsx b/src/renderer/components/pages/PlayerStatsPage.tsx
index 8b557b8..ed2a623 100644
--- a/src/renderer/components/pages/PlayerStatsPage.tsx
+++ b/src/renderer/components/pages/PlayerStatsPage.tsx
@@ -138,6 +138,9 @@ export default function PlayerStatsPage() {
     followedPlayers,
     addFollowedPlayer,
     removeFollowedPlayer,
+    blockedPlayers,
+    addBlockedPlayer,
+    removeBlockedPlayer,
   } = EvosStore();
 
   /** URL search parameters from the current location */
@@ -254,6 +257,18 @@ export default function PlayerStatsPage() {
     }
   };
 
+  /** Check if the current player is blocked */
+  const isBlocked = blockedPlayers.includes(playerSearch);
+
+  /** Handle block/unblock action */
+  const handleBlockToggle = () => {
+    if (isBlocked) {
+      removeBlockedPlayer(playerSearch);
+    } else {
+      addBlockedPlayer(playerSearch);
+    }
+  };
+
   return (
     <>
       {error && (
@@ -274,7 +289,7 @@ export default function PlayerStatsPage() {
       >
         {/* Player Profile and Follow Button */}
         <Grid container alignItems="center" spacing={2}>
-          <Grid size={8}>
+          <Grid size={7}>
             {!playerData ? (
               <Skeleton
                 variant="rectangular"
@@ -292,7 +307,7 @@ export default function PlayerStatsPage() {
             )}
           </Grid>
           <Grid
-            size={4}
+            size={5}
             sx={{
               display: 'flex',
               justifyContent: 'flex-end',
@@ -302,8 +317,8 @@ export default function PlayerStatsPage() {
           >
             {playerSearch && (
               <Button
-                variant="outlined"
-                color="primary"
+                variant="contained"
+                sx={{ whiteSpace: 'nowrap' }}
                 onClick={() =>
                   navigate(`/quests?player=${encodeURIComponent(playerSearch)}`)
                 }
@@ -322,6 +337,15 @@ export default function PlayerStatsPage() {
                   : t('menuOptions.Follow')}
               </Button>
             )}
+            {playerSearch && playerSearch !== activeUser?.handle && (
+              <Button
+                variant="contained"
+                color={isBlocked ? 'error' : 'warning'}
+                onClick={handleBlockToggle}
+              >
+                {isBlocked ? t('menuOptions.Unblock') : t('menuOptions.Block')}
+              </Button>
+            )}
           </Grid>
         </Grid>
 
diff --git a/src/renderer/components/pages/SettingsPage.tsx b/src/renderer/components/pages/SettingsPage.tsx
index c5a834e..06a9d0d 100644
--- a/src/renderer/components/pages/SettingsPage.tsx
+++ b/src/renderer/components/pages/SettingsPage.tsx
@@ -72,6 +72,8 @@ export default function SettingsPage() {
     setIp,
     showAllChat,
     setShowAllChat,
+    hideChat,
+    setHideChat,
     enableDiscordRPC,
     toggleDiscordRPC,
     setGameExpanded,
@@ -350,6 +352,8 @@ export default function SettingsPage() {
           <AppearanceSection
             mode={mode}
             toggleMode={toggleMode}
+            hideChat={hideChat}
+            setHideChat={(v: string) => setHideChat(v)}
             colorPrimary={colorPrimary}
             setColorPrimary={handleChangeColorPrimary}
             colorSecondary={colorSecondary}
diff --git a/src/renderer/components/settings/AppearanceSection.tsx b/src/renderer/components/settings/AppearanceSection.tsx
index 59ef12f..1459a07 100644
--- a/src/renderer/components/settings/AppearanceSection.tsx
+++ b/src/renderer/components/settings/AppearanceSection.tsx
@@ -7,6 +7,8 @@ import { useTranslation } from 'react-i18next';
 type Props = {
   mode: string;
   toggleMode: () => void;
+  hideChat: string;
+  setHideChat: (v: string) => void;
   colorPrimary: string;
   setColorPrimary: (v: string) => void;
   colorSecondary: string;
@@ -26,6 +28,8 @@ export default function AppearanceSection(props: Props) {
   const {
     mode,
     toggleMode,
+    hideChat,
+    setHideChat,
     colorPrimary,
     setColorPrimary,
     colorSecondary,
@@ -108,6 +112,14 @@ export default function AppearanceSection(props: Props) {
           </Grid>
         </Grid>
       </Box>
+      <FormGroup>
+        <FormControlLabel
+          control={<Switch />}
+          label={t('settings.hideChatLabel')}
+          checked={hideChat === 'true'}
+          onChange={() => setHideChat(hideChat === 'true' ? 'false' : 'true')}
+        />
+      </FormGroup>
     </SectionCard>
   );
 }
diff --git a/src/renderer/hooks/useChatWebSocket.ts b/src/renderer/hooks/useChatWebSocket.ts
index d9f75b9..521e7c0 100644
--- a/src/renderer/hooks/useChatWebSocket.ts
+++ b/src/renderer/hooks/useChatWebSocket.ts
@@ -8,6 +8,7 @@
 
 import { useState, useRef, useCallback } from 'react';
 import useWebSocket, { ReadyState } from 'react-use-websocket';
+import EvosStore from '../lib/EvosStore';
 import { CHAT_WS_URL } from '../lib/Evos';
 
 export interface ChatMessage {
@@ -125,6 +126,12 @@ export default function useChatWebSocket({
 
   const sendMessage = useCallback(
     (text: string, to: string) => {
+      const blockedPlayers = EvosStore.getState().blockedPlayers;
+      if (blockedPlayers.includes(to)) {
+        // eslint-disable-next-line no-console
+        console.warn('Cannot send message to blocked player:', to);
+        return;
+      }
       if (!text.trim() || !handle || !to) return;
       sendJsonMessage({
         type: 'CHAT',
diff --git a/src/renderer/lib/EvosStore.ts b/src/renderer/lib/EvosStore.ts
index 8155859..52d1744 100644
--- a/src/renderer/lib/EvosStore.ts
+++ b/src/renderer/lib/EvosStore.ts
@@ -120,6 +120,8 @@ export interface EvosStoreState {
   noLogEnabled: string;
   showAllChat: string;
   setShowAllChat: (showAllChat: string) => Promise<void>;
+  hideChat: string;
+  setHideChat: (hideChat: string) => Promise<void>;
   setIsDownloading: (isDownloading: boolean) => void;
   init: () => void;
   toggleMode: () => void;
@@ -173,6 +175,10 @@ export interface EvosStoreState {
   setFollowedPlayers: (players: string[]) => Promise<void>;
   addFollowedPlayer: (player: string) => Promise<void>;
   removeFollowedPlayer: (player: string) => Promise<void>;
+  blockedPlayers: string[];
+  setBlockedPlayers: (players: string[]) => Promise<void>;
+  addBlockedPlayer: (player: string) => Promise<void>;
+  removeBlockedPlayer: (player: string) => Promise<void>;
 }
 
 const EvosStore = create<EvosStoreState>((set, get) => ({
@@ -196,6 +202,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
   isDownloading: false,
   noLogEnabled: 'false',
   showAllChat: 'true',
+  hideChat: 'false',
   discordId: 0,
   enableDiscordRPC: 'true',
   gameExpanded: 'true',
@@ -207,6 +214,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
   nobranchDownload: false,
   apiVersion: 'production',
   followedPlayers: [],
+  blockedPlayers: [],
 
   setStats: async (stats: string) => {
     set({ stats });
@@ -336,6 +344,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       ticketEnabled,
       noLogEnabled,
       showAllChat,
+      hideChat,
       enableDiscordRPC,
       gameExpanded,
       branch,
@@ -348,6 +357,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       colorPaper,
       apiVersion,
       followedPlayers,
+      blockedPlayers,
     ] = await Promise.all([
       get().getFromStorage('mode') as string,
       get().getFromStorage('authenticatedUsers') as AuthUser[],
@@ -358,6 +368,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       get().getFromStorage('ticketEnabled') as string,
       get().getFromStorage('noLogEnabled') as string,
       get().getFromStorage('showAllChat') as string,
+      get().getFromStorage('hideChat') as string,
       get().getFromStorage('enableDiscordRPC') as string,
       get().getFromStorage('gameExpanded') as string,
       get().getFromStorage('branch') as string,
@@ -373,6 +384,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       get().getFromStorage('colorPaper') as string,
       get().getFromStorage('apiVersion') as 'v1' | 'production',
       get().getFromStorage('followedPlayers') as string[],
+      get().getFromStorage('blockedPlayers') as string[],
     ]);
 
     let users: AuthUser[] = [];
@@ -418,6 +430,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       ticketEnabled: ticketEnabled || 'true',
       noLogEnabled: noLogEnabled || 'false',
       showAllChat: showAllChat || 'true',
+      hideChat: hideChat || 'false',
       enableDiscordRPC: enableDiscordRPC || 'true',
       gameExpanded: gameExpanded || 'true',
       branch: branch || 'Original',
@@ -430,6 +443,7 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
       colorPaper: colorPaper || '#0000',
       apiVersion: apiVersion || 'production',
       followedPlayers: followedPlayers || [],
+      blockedPlayers: blockedPlayers || [],
     });
     get().switchUser(activeUser?.user || users[0]?.user || '');
   },
@@ -443,6 +457,11 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
     await get().setToStorage('showAllChat', showAllChat);
   },
 
+  setHideChat: async (hideChat: string) => {
+    set({ hideChat });
+    await get().setToStorage('hideChat', hideChat);
+  },
+
   toggleMode: async () => {
     const newMode = get().mode === 'dark' ? 'light' : 'dark';
     set({ mode: newMode });
@@ -634,6 +653,24 @@ const EvosStore = create<EvosStoreState>((set, get) => ({
     set({ followedPlayers: updatedPlayers });
     await get().setToStorage('followedPlayers', updatedPlayers);
   },
+  setBlockedPlayers: async (players: string[]) => {
+    set({ blockedPlayers: players });
+    await get().setToStorage('blockedPlayers', players);
+  },
+  addBlockedPlayer: async (player: string) => {
+    const currentPlayers = get().blockedPlayers;
+    if (!currentPlayers.includes(player)) {
+      const updatedPlayers = [...currentPlayers, player];
+      set({ blockedPlayers: updatedPlayers });
+      await get().setToStorage('blockedPlayers', updatedPlayers);
+    }
+  },
+  removeBlockedPlayer: async (player: string) => {
+    const currentPlayers = get().blockedPlayers;
+    const updatedPlayers = currentPlayers.filter((p) => p !== player);
+    set({ blockedPlayers: updatedPlayers });
+    await get().setToStorage('blockedPlayers', updatedPlayers);
+  },
 }));
 
 // Call the init function to fetch and set the values from storage.
diff --git a/src/renderer/locales/br/translation.json b/src/renderer/locales/br/translation.json
index e3edc87..9e806a8 100644
--- a/src/renderer/locales/br/translation.json
+++ b/src/renderer/locales/br/translation.json
@@ -18,7 +18,9 @@
     "wiki": "Wiki",
     "SupportUs": "Apoie-nos",
     "folowedPlayers": "Jogadores Seguidos",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Bloquear",
+    "Unblock": "Desbloquear"
   },
   "followedPlayers": {
     "title": "Jogadores Seguidos",
@@ -77,7 +79,9 @@
     "communityTitle": "Chat da Comunidade",
     "subtitle": "Converse com outros jogadores online",
     "notsaved": "As conversas não são salvas entre as sessões",
+    "warning": "Relate qualquer abuso à equipe via Discord.",
     "placeholder": "Digite uma mensagem...",
+    "blockedUser": "Você bloqueou este usuário",
     "send": "Enviar",
     "online": "online",
     "noMessages": "Ainda não há mensagens. Seja o primeiro a dizer oi!",
@@ -344,7 +348,8 @@
     "labelBackgroundColor": "Cor de Fundo",
     "labelTextColor": "Cor do Texto",
     "labelScrollbarColor": "Cor da Barra de Rolagem",
-    "statsVersion": "Versão das Estatísticas"
+    "statsVersion": "Versão das Estatísticas",
+    "hideChatLabel": "Esconder-se do chat (desativa conexão do chat)"
   },
   "maps": {
     "map": "Mapa",
diff --git a/src/renderer/locales/de/translation.json b/src/renderer/locales/de/translation.json
index 8e7bbb7..c97bee7 100644
--- a/src/renderer/locales/de/translation.json
+++ b/src/renderer/locales/de/translation.json
@@ -21,7 +21,9 @@
     "Follow": "Folgen",
     "Unfollow": "Entfolgen",
     "viewQuests": "Quests ansehen",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Blockieren",
+    "Unblock": "Entsperren"
   },
   "followedPlayers": {
     "title": "Gefolgte Spieler",
@@ -80,7 +82,9 @@
     "communityTitle": "Community-Chat",
     "subtitle": "Chatte mit anderen Spielern online",
     "notsaved": "Chats werden nicht zwischen den Sitzungen gespeichert",
-    "placeholder": "Schreibe eine Nachricht...",
+    "warning": "Melden Sie Missbrauch über Discord an die Mitarbeiter.",
+    "placeholder": "Nachricht eingeben...",
+    "blockedUser": "Du hast diesen Benutzer blockiert",
     "send": "Senden",
     "online": "online",
     "noMessages": "Noch keine Nachrichten. Sei der Erste, der Hallo sagt!",
@@ -198,7 +202,8 @@
     "labelBackgroundColor": "Hintergrundfarbe",
     "labelTextColor": "Textfarbe",
     "labelScrollbarColor": "Scrollleistenfarbe",
-    "statsVersion": "Statistik-Version"
+    "statsVersion": "Statistik-Version",
+    "hideChatLabel": "Vom Chat verstecken (deaktiviert Chat-Verbindung)"
   },
   "titles": {
     "-1": "",
diff --git a/src/renderer/locales/en/translation.json b/src/renderer/locales/en/translation.json
index 6f62b50..92aebdf 100644
--- a/src/renderer/locales/en/translation.json
+++ b/src/renderer/locales/en/translation.json
@@ -22,7 +22,9 @@
     "Unfollow": "Unfollow",
     "quests": "Quests",
     "viewQuests": "View Quests",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Block",
+    "Unblock": "Unblock"
   },
   "followedPlayers": {
     "title": "Followed Players",
@@ -81,10 +83,12 @@
     "communityTitle": "Community Chat",
     "subtitle": "Chat with other players online",
     "notsaved": "Chats are not saved between sessions",
+    "warning": "Report any abuse to staff via Discord.",
     "placeholder": "Type a message...",
     "send": "Send",
     "online": "online",
     "noMessages": "No messages yet. Be the first to say hello!",
+    "blockedUser": "You have blocked this user",
     "connecting": "Connecting...",
     "connected": "Connected",
     "disconnected": "Disconnected"
@@ -207,7 +211,8 @@
     "labelBackgroundColor": "Background Color",
     "labelTextColor": "Text Color",
     "labelScrollbarColor": "Scrollbar Color",
-    "statsVersion": "Stats Version"
+    "statsVersion": "Stats Version",
+    "hideChatLabel": "Hide from Chat (disables chat connection)"
   },
   "titles": {
     "-1": "",
diff --git a/src/renderer/locales/es/translation.json b/src/renderer/locales/es/translation.json
index ee6b5cd..b76dad9 100644
--- a/src/renderer/locales/es/translation.json
+++ b/src/renderer/locales/es/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Jugadores Seguidos",
     "Follow": "Seguir",
     "Unfollow": "Dejar de seguir",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Bloquear",
+    "Unblock": "Desbloquear"
   },
   "followedPlayers": {
     "title": "Jugadores Seguidos",
@@ -79,7 +81,9 @@
     "communityTitle": "Chat de la Comunidad",
     "subtitle": "Chatea con otros jugadores en línea",
     "notsaved": "Los chats no se guardan entre sesiones",
+    "warning": "Informe cualquier abuso al personal a través de Discord.",
     "placeholder": "Escribe un mensaje...",
+    "blockedUser": "Has bloqueado a este usuario",
     "send": "Enviar",
     "online": "en línea",
     "noMessages": "Aún no hay mensajes. ¡Sé el primero en saludar!",
@@ -314,7 +318,8 @@
     "labelBackgroundColor": "Color de Fondo",
     "labelTextColor": "Color del Texto",
     "labelScrollbarColor": "Color de la Barra de Desplazamiento",
-    "statsVersion": "Versión de Estadísticas"
+    "statsVersion": "Versión de Estadísticas",
+    "hideChatLabel": "Ocultarse del chat (desactiva la conexión al chat)"
   },
   "maps": {
     "map": "Mapa",
diff --git a/src/renderer/locales/fr/translation.json b/src/renderer/locales/fr/translation.json
index 7e69535..97640cf 100644
--- a/src/renderer/locales/fr/translation.json
+++ b/src/renderer/locales/fr/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Joueurs Suivis",
     "Follow": "Suivre",
     "Unfollow": "Ne plus suivre",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Bloquer",
+    "Unblock": "Débloquer"
   },
   "followedPlayers": {
     "title": "Joueurs Suivis",
@@ -79,7 +81,9 @@
     "communityTitle": "Chat Communautaire",
     "subtitle": "Discutez avec d'autres joueurs en ligne",
     "notsaved": "Les discussions ne sont pas sauvegardées entre les sessions",
+    "warning": "Signalez tout abus au personnel via Discord.",
     "placeholder": "Tapez un message...",
+    "blockedUser": "Vous avez bloqué cet utilisateur",
     "send": "Envoyer",
     "online": "en ligne",
     "noMessages": "Pas encore de messages. Soyez le premier à dire bonjour !",
@@ -346,7 +350,8 @@
     "labelBackgroundColor": "Couleur de Fond",
     "labelTextColor": "Couleur du Texte",
     "labelScrollbarColor": "Couleur de la Barre de Défilement",
-    "statsVersion": "Version des Statistiques"
+    "statsVersion": "Version des Statistiques",
+    "hideChatLabel": "Se cacher du chat (désactive la connexion au chat)"
   },
   "maps": {
     "map": "Map",
diff --git a/src/renderer/locales/it/translation.json b/src/renderer/locales/it/translation.json
index 4c9489e..ff7b0c0 100644
--- a/src/renderer/locales/it/translation.json
+++ b/src/renderer/locales/it/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Giocatori Seguiti",
     "Follow": "Segui",
     "Unfollow": "Smetti di seguire",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Blocca",
+    "Unblock": "Sblocca"
   },
   "followedPlayers": {
     "title": "Giocatori Seguiti",
@@ -79,7 +81,9 @@
     "communityTitle": "Chat della Community",
     "subtitle": "Chatta con altri giocatori online",
     "notsaved": "Le chat non vengono salvate tra le sessioni",
+    "warning": "Segnala eventuali abusi allo staff tramite Discord.",
     "placeholder": "Scrivi un messaggio...",
+    "blockedUser": "Hai bloccato questo utente",
     "send": "Invia",
     "online": "online",
     "noMessages": "Ancora nessun messaggio. Sii il primo a salutare!",
@@ -318,7 +322,8 @@
     "labelBackgroundColor": "Colore di Sfondo",
     "labelTextColor": "Colore del Testo",
     "labelScrollbarColor": "Colore della Barra di Scorrimento",
-    "statsVersion": "Versione Statistiche"
+    "statsVersion": "Versione Statistiche",
+    "hideChatLabel": "Nasconditi dalla chat (disabilita connessione chat)"
   },
   "maps": {
     "map": "Mappa",
diff --git a/src/renderer/locales/nl/translation.json b/src/renderer/locales/nl/translation.json
index dd9ffb0..3a753b7 100644
--- a/src/renderer/locales/nl/translation.json
+++ b/src/renderer/locales/nl/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Gevolgde Spelers",
     "Follow": "Volgen",
     "Unfollow": "Ontvolgen",
-    "chat": "Chat"
+    "chat": "Chat",
+    "Block": "Blokkeren",
+    "Unblock": "Deblokkeren"
   },
   "followedPlayers": {
     "title": "Gevolgde Spelers",
@@ -79,7 +81,9 @@
     "communityTitle": "Community Chat",
     "subtitle": "Chat met andere online spelers",
     "notsaved": "Gesprekken worden niet opgeslagen tussen sessies",
+    "warning": "Meld misbruik aan het personeel via Discord.",
     "placeholder": "Type een bericht...",
+    "blockedUser": "Je hebt deze gebruiker geblokkeerd",
     "send": "Versturen",
     "online": "online",
     "noMessages": "Nog geen berichten. Wees de eerste om hallo te zeggen!",
@@ -346,7 +350,8 @@
     "labelBackgroundColor": "Achtergrondkleur",
     "labelTextColor": "Tekstkleur",
     "labelScrollbarColor": "Scrollbalkkleur",
-    "statsVersion": "Statistieken Versie"
+    "statsVersion": "Statistieken Versie",
+    "hideChatLabel": "Zich verbergen voor de chat (chat-verbinding uitschakelen)"
   },
   "maps": {
     "map": "Map",
diff --git a/src/renderer/locales/ru/translation.json b/src/renderer/locales/ru/translation.json
index 58edadc..c3af4f5 100644
--- a/src/renderer/locales/ru/translation.json
+++ b/src/renderer/locales/ru/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Отслеживаемые игроки",
     "Follow": "Подписаться",
     "Unfollow": "Отписаться",
-    "chat": "Чат"
+    "chat": "Chat",
+    "Block": "Заблокировать",
+    "Unblock": "Разблокировать"
   },
   "followedPlayers": {
     "title": "Отслеживаемые игроки",
@@ -79,7 +81,9 @@
     "communityTitle": "Общий чат",
     "subtitle": "Общайтесь с другими игроками в сети",
     "notsaved": "Чаты не сохраняются между сессиями",
+    "warning": "Сообщайте о любых нарушениях персоналу через Discord.",
     "placeholder": "Введите сообщение...",
+    "blockedUser": "Вы заблокировали этого пользователя",
     "send": "Отправить",
     "online": "в сети",
     "noMessages": "Сообщений пока нет. Будьте первым, кто скажет привет!",
@@ -333,7 +337,8 @@
     "labelBackgroundColor": "Цвет Фона",
     "labelTextColor": "Цвет Текста",
     "labelScrollbarColor": "Цвет Полосы Прокрутки",
-    "statsVersion": "Версия Статистики"
+    "statsVersion": "Версия Статистики",
+    "hideChatLabel": "Скрыться из чата (отключает подключение к чату)"
   },
   "maps": {
     "map": "Карта",
diff --git a/src/renderer/locales/tr/translation.json b/src/renderer/locales/tr/translation.json
index 7556c62..2926386 100644
--- a/src/renderer/locales/tr/translation.json
+++ b/src/renderer/locales/tr/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "Takip Edilen Oyuncular",
     "Follow": "Takip Et",
     "Unfollow": "Takibi Bırak",
-    "chat": "Sohbet"
+    "chat": "Sohbet",
+    "Block": "Engelle",
+    "Unblock": "Engeli Kaldır"
   },
   "followedPlayers": {
     "title": "Takip Edilen Oyuncular",
@@ -79,7 +81,9 @@
     "communityTitle": "Topluluk Sohbeti",
     "subtitle": "Diğer çevrimiçi oyuncularla sohbet edin",
     "notsaved": "Sohbetler oturumlar arasında kaydedilmez",
+    "warning": "Herhangi bir kötüye kullanımı Discord üzerinden yetkililere bildirin.",
     "placeholder": "Bir mesaj yazın...",
+    "blockedUser": "Bu kullanıcıyı engellediniz",
     "send": "Gönder",
     "online": "çevrimiçi",
     "noMessages": "Henüz mesaj yok. Merhaba diyen ilk kişi siz olun!",
@@ -197,7 +201,8 @@
     "labelBackgroundColor": "Arka Plan Rengi",
     "labelTextColor": "Metin Rengi",
     "labelScrollbarColor": "Kaydırma Çubuğu Rengi",
-    "statsVersion": "İstatistik Sürümü"
+    "statsVersion": "İstatistik Sürümü",
+    "hideChatLabel": "Sohbetten Gizle (sohbet bağlantısını devre dışı bırakır)"
   },
   "titles": {
     "-1": "",
diff --git a/src/renderer/locales/zh/translation.json b/src/renderer/locales/zh/translation.json
index e911e6e..bcd2321 100644
--- a/src/renderer/locales/zh/translation.json
+++ b/src/renderer/locales/zh/translation.json
@@ -20,7 +20,9 @@
     "folowedPlayers": "关注的玩家",
     "Follow": "关注",
     "Unfollow": "取消关注",
-    "chat": "聊天"
+    "chat": "聊天",
+    "Block": "屏蔽",
+    "Unblock": "取消屏蔽"
   },
   "followedPlayers": {
     "title": "关注的玩家",
@@ -79,7 +81,9 @@
     "communityTitle": "社区聊天",
     "subtitle": "与其他在线玩家聊天",
     "notsaved": "会话之间不会保存聊天记录",
+    "warning": "请通过 Discord 向工作人员报告任何滥用行为。",
     "placeholder": "输入消息...",
+    "blockedUser": "您已屏蔽此用户",
     "send": "发送",
     "online": "在线",
     "noMessages": "暂无消息。成为第一个打招呼的人吧！",
@@ -196,7 +200,8 @@
     "labelBackgroundColor": "背景颜色",
     "labelTextColor": "文字颜色",
     "labelScrollbarColor": "滚动条颜色",
-    "statsVersion": "统计版本"
+    "statsVersion": "统计版本",
+    "hideChatLabel": "从聊天中隐藏（禁用聊天连接）"
   },
   "titles": {
     "-1": "",
